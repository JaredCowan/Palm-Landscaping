window.location.hash = "";
console.log('js ajax loaded');
<% replaceElm = "#{params[:replace]}" %>
var $main_content = $("#<%= replaceElm %>");
<% filePath = params[:hard].gsub("_", "/").gsub("-", "_") %>
$main_content.replaceWith('<%= escape_javascript(render partial: "/#{filePath}") %>');
window.location.hash = "<%= replaceElm %>";
